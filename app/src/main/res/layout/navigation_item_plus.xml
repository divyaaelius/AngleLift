<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:clipToPadding="false"
    android:focusable="false"
    android:paddingRight="@dimen/default_padding_half">

    <RelativeLayout
        android:id="@+id/containerNavigationBarPlus"
        android:layout_width="@dimen/btn_size_plus_taxi_w"
        android:layout_height="@dimen/btn_size_map"
        android:layout_margin="@dimen/default_padding_quarter"
        android:background="@drawable/map_button_add_background"
        android:clipToPadding="false">

        <!--<com.mytaxi.passenger.shared.view.TintableTextView
            android:id="@id/txtAddBooking"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:drawableLeft="@drawable/ic_andanotherone"
            android:drawablePadding="4.0dip"
            android:drawableStart="@drawable/ic_andanotherone"
            android:ellipsize="none"
            android:paddingLeft="14.0dip"
            android:paddingRight="18.0dip"
            android:textSize="13.0sp"
            app:drawableTint="@color/primary_tint" />-->
    </RelativeLayout>
</RelativeLayout>